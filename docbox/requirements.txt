# Core — required
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
httpx>=0.26
pydantic>=2.5

# Scanning — required for edge detection, dewarping and the enhancement filters.
# Without these the scanner degrades to contrast/sharpening only (see SCANNING.md).
opencv-python-headless>=4.9
numpy>=1.26

# Documents — PDF text, image handling, multi-page PDF assembly
pypdf>=4.0
pillow>=10.0

# iPhone HEIC photos. Needs libheif on the host; optional but recommended.
pillow-heif>=0.15

# Google Drive import. Only needed with DOCBOX_GDRIVE_ENABLED=true and a
# service-account key; an OAuth refresh token needs no extra package.
google-auth>=2.28

# Dev
pytest>=8.0
