// Copy this file to `config.js` and fill in your Supabase project details.
// (Settings → API in your Supabase dashboard.)
//
// The anon key is safe to expose in the browser — Row Level Security is what
// actually protects your data.
window.APP_CONFIG = {
  SUPABASE_URL: 'https://YOUR-PROJECT-ref.supabase.co',
  SUPABASE_ANON_KEY: 'YOUR-PUBLIC-ANON-KEY',
  // Display only; the app stores raw amounts and formats with this currency.
  DEFAULT_CURRENCY: 'USD',
};
